#include <stdio.h>
#define DISTANCE 345
#define speed DISTANCE / seconds
// fix the program so it prints out the correct result!

int main()
{
    int seconds = 17;

    printf("%f", (float)speed);

    return 0;
}